x:=3;
let
  var num : Int;
  var a : Int;
  var b : Int;
  var temp : Int
in
begin
  num := 5;
  a := 1;
  b := 0;
  while (num) do
  begin
    temp := a;
    a := a + b - c * 2 / 5;
    b := temp + a;
    num := num - 1
  end
end